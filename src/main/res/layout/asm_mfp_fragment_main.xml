<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <variable
            name="bindingCallback"
            type="com.alcodes.alcodessmmediafilepicker.databinding.bindingcallbacks.MainBindingCallback" />

    </data>


    <LinearLayout
        android:layout_width="match_parent"
        android:gravity="center"

        android:layout_height="match_parent"
        android:orientation="vertical">
        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:onClick="@{() -> bindingCallback.onCustomPickerButtonClicked()}"

            android:text="Custom File Picker"/>
        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"

            android:onClick="@{() -> bindingCallback.onImageFilePickerButtonClicked()}"
            android:text="Github Sample file picker" />

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"

            android:onClick="@{() -> bindingCallback.onListViewFilePickerButtonClicked()}"
            android:text="List View File Picker" />


    </LinearLayout>

</layout>
